See also [[Ledger Hardware Wallet]].

sudo journalctl -f

Attach ledger to ledger VM.

Log example if it works. (Meaning, after seeing the following output it was possible to connect ledger to ledger manager.)

<pre>

</pre>
