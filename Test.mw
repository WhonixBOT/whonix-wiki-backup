{{DISPLAYTITLE:Testers}}
{{Header}}
{{#seo:
|description=Help Testing Whonix
|image=https://www.whonix.org/w/images/2/2e/Board-361516-640.jpg
}}

= Introduction =

{{mbox
| type      = notice
| image   = [[File:Ambox_notice.png|40px|alt=Info]]
| text       = Advanced users, developers and willing testers only.
}}


{{Test}}

= Whonix-Gateway and Whonix-Workstation =

== Miscellaneous ==
Check locale.

{{CodeSelect|code=
locale
}}

Check apt config and see if periodic updates are disabled.

{{CodeSelect|code=
apt-config dump
}}

Install a new kernel for testing purposes.

<pre>.</pre>

Check the content of {{Code2|/etc/network/interfaces}}

{{CodeSelect|code=
cat /etc/network/interfaces
}}

Check the content of {{Code2|/etc/resolv.conf}}

{{CodeSelect|code= 
cat /etc/resolv.conf
}}

Check {{Code2|/etc/apt/sources.list}}

{{CodeSelect|code=
cat /etc/apt/sources.list
}}

Check iptables.

{{CodeSelect|code=
sudo iptables-save-deterministic
}}

Reboot from terminal while X is running.

<pre>
Switch to terminal.
</pre>

Reboot.

{{CodeSelect|code=
sudo reboot
}}

No errors should appear like "failed to kill service".

== Extra Tests ==
Check if aptitude is functional.

{{CodeSelect|code=
sudo aptitude update
}}

See the footnotes if additional manual tests are preferred. <ref>
These checks are not as important because relevant messages would probably be shown during <code>sudo systemctl list-units --failed</code>.

Check if /var/run/bootclockrandomization/success exists.

{{CodeSelect|code=
ls -la /var/run/bootclockrandomization/success
}}

Check the boot clock randomization log.

{{CodeSelect|code=
cat /var/log/bootclockrandomization.log
}}
{{CodeSelect|code=
sudo service bootclockrandomization status
}}
{{CodeSelect|code=
echo $?
}}

Check if /var/run/timesanitycheck/success exists.

{{CodeSelect|code=
ls -la /var/run/timesanitycheck/success
}}

Inspect the time sanity check log.

{{CodeSelect|code=
cat /var/log/timesanitycheck.log
}}

Confirm the time sanity check status.

{{CodeSelect|code=
sudo service timesanitycheck status
}}
{{CodeSelect|code=
echo $?
}}
</ref> <ref>These checks are not as important because sdwdate-gui would likely identify any issues beforehand.

Check if /var/run/sdwdate/success exists.

{{CodeSelect|code=
ls -la /var/run/sdwdate/success
}}

Check the sdwdate log.

{{CodeSelect|code=
cat /var/log/sdwdate.log
}}

Check the sdwdate status.

{{CodeSelect|code=
sudo service sdwdate status
}}
{{CodeSelect|code=
echo $?
}}
</ref>

Test the re-installation of x11-common.

{{CodeSelect|code=
sudo apt-get install --reinstall x11-common
}}

== KDM ==
[[Non-Qubes-Whonix]] only:

Check kdm stops and restarts correctly.

{{CodeSelect|code=
sudo service kdm stop
}}

{{CodeSelect|code=
sudo service kdm start
}}

= Whonix-Gateway Tests =

== Tor Tests ==

Check the Tor version.

{{CodeSelect|code=
anon-info
}}

Check the obfsproxy version; it must include obfs4. <ref>https://trac.torproject.org/projects/tor/ticket/18072</ref>

{{CodeSelect|code=
obfsproxy -h
}}

Check Tor logs.

{{CodeSelect|code=
less /var/log/tor/log
}}

Check Tor warnings.

{{CodeSelect|code=
grep warn /var/log/tor/log
}}

The message {{Code2|[warn] Socks version 71 not recognized. (Tor is not an http proxy.)}} [[Tor#Non-Issues|can be safely ignored]].

== Clock Skew ==

Check for clock skew.

{{CodeSelect|code=
grep clock /var/log/tor/log
}}

== arm ==

Test if arm is fully functional.

{{CodeSelect|code=
arm
}}

Test if arm's New Identity function is working.

{{CodeSelect|code=
.
}}

== Miscellaneous ==

After logging in, the Whonix help/welcome/disclaimer message should appear.

{{CodeSelect|code=
.
}}

Test obfsproxy bridge connectivity is functional.

{{CodeSelect|code=
.
}}

Check ''/var/log/syslog'' for AppArmor error messages.

{{CodeSelect|code=
grep DENIED /var/log/syslog
}}

= Whonix-Workstation Tests =

== Basic Tests ==

Ping the Whonix-Gateway; this will <u>not</u> work.

<pre>
# You will not be able to ping the Whonix-Gateway,
# because ICMP is blocked by the firewall.
# If you want to test it, you have to adjust the firewall,
# or to deactivate the firewall while testing.
</pre>
{{CodeSelect|code=
ping 10.152.152.10
}}

Power off Whonix-Gateway. Try to ping outside or to use the browser in Whonix-Workstation. Obviously, should NOT work.

<pre>
.
</pre>

Power on Whonix-Gateway again. Visit https://check.torproject.org/ with Tor Browser. You should see a “Congratulations”.

<pre>
.
</pre>

Use a Tor Browser to visit a .onion address (Try the [http://idnxcnkne4qt76tg.onion torproject.org onion service])

<pre>
http://idnxcnkne4qt76tg.onion
</pre>

Test Tor Button's New Identity Feature.

<pre>
.
</pre>

Note: Ping commands should NOT work for external addresses from your Whonix-Workstation, ICMP traffic<ref>http://en.wikipedia.org/wiki/Internet_Control_Message_Protocol</ref> is not proxied, and filtered by Whonix's Firewall ({{WhonixFirewall}}), because Tor does not support UDP.

<pre>
.
</pre>

{{Code2|dig google.com}} must only return a single IP, compare with the output on Whonix-Gateway or Host.

{{CodeSelect|code=
dig google.com
}}

Test gpg. Example.

{{CodeSelect|code=
gpg --keyserver keys.gnupg.net --recv A3C4F0F979CAA22CDBA8F512EE8CBC9E886DDD89
}}

Test curl uwt wrapper.

{{CodeSelect|code=
curl http://idnxcnkne4qt76tg.onion
}}

{{CodeSelect|code=
whonixcheck --leak-tests
}}

<ref>
Obsolete because of <code>whonixcheck --leak-tests</code>.

Test curl through TransPort.

{{CodeSelect|code=
{{Curl Plain}} http://idnxcnkne4qt76tg.onion
}}
</ref>

Setup an onion service on Whonix-Gateway and test if it works. You can access your own test onion service using Tor Browser.

<pre>.</pre>

See if whonixcheck gets autostarted.

<pre>.</pre>

Test HexChat, connect to a an SSL protected IRC server.

<pre>.</pre>

test HexChat, connect to a hidden IRC server.

<pre>.</pre>

Install lighttpd.

{{CodeSelect|code=
sudo apt-get install lighttpd
}}

Restart lighttpd.

{{CodeSelect|code=
sudo service lighttpd restart
}}

Try to download the local index.html.

{{CodeSelect|code=
curl 127.0.0.1
}}

Check.

{{CodeSelect|code=
cat index.html
}}

Let's check if git is working. A good testing target would be a hidden git server. Therefore, check if [http://wzrtr6gpencksu3d.onion Gittor] is online by visiting its list of [http://wzrtr6gpencksu3d.onion/gitlab/public public Gittor repositories]. Test it.

{{CodeSelect|code=
git clone http://wzrtr6gpencksu3d.onion/gitlab/w00t/pgp-auth.git
}}

Check if regular git servers are reachable as well.

{{CodeSelect|code=
git clone https://github.com/Whonix/Whonix
}}

== Default Browser ==

=== Quick Launcher ===
Check if the Tor Browser quick launcher (fav icon) next to the start menu button is visible and startable.

=== Text Links ===
1. Open Konsole.

2. Run the following command.

{{CodeSelect|code=
echo http://127.0.0.1
}}

3. Right click on the echoed {{Code2|http://127.0.0.1}} and choose open link.

4. Check if it opens asks for confirmation to open that file in Tor Browser. Check if nothing happens, when pressing {{Code2|No}} (which should be the default!) and check if it opens a new Tor Browser window when pressing {{Code2|Yes}}.

=== File Links ===
1. Create a file {{Code2|~/test.html}} with the following content.

<pre>
test
</pre>

2. Open Dolphin (default file manager) and double click on that file.

3. Check if it opens asks for confirmation to open that file in Tor Browser.

=== Terminal Tests ===
1. Open Konsole.

2. Run the following command.

{{CodeSelect|code=
x-www-browser http://127.0.0.1
}}

3. Check if it asks for confirmation to open that file in Tor Browser.

4. Check the same for.

{{CodeSelect|code=
gnome-www-browser http://127.0.0.1
}}

5. Check the same for.

{{CodeSelect|code=
xdg-open http://127.0.0.1
}}

6. Check the same for.

{{CodeSelect|code=
gnome-open http://127.0.0.1
}}

7. Next, remove open-link-confirmation.

{{CodeSelect|code=
sudo apt-get remove open-link-confirmation
}}

And repeat the tests above.

== Applications ==

* [[Metadata]]
* [[Mixmaster]]
* [[Tor Browser]]
* [[Manually_Downloading_Tor_Browser|Manually Downloading Tor Browser]]: Check if Tor Browser runs in Whonix out of the box (without using the {{Code|torbrowser}} script).

== Leak Tests ==
See [[Dev/Leak Tests]].

= Footnotes =
{{reflist|close=1}}

{{Footer}}

[[Category:Development]] [[Category:Design]] [[Category:Documentation]]
