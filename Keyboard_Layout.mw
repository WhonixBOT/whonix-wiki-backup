{{Header}}
__FORCETOC__
{{#seo:
|description=How to change the Keyboard Layout in {{project_name}}.
|image=https://www.whonix.org/w/images/e/ee/Keyboardlayout1282268640.jpg
}}

= {{non_q_project_name_short}} =
== Terminal ==

To change the <u>keyboard layout</u>.

<pre>
sudo dpkg-reconfigure console-data

## French keyboards:
## Perhaps an upstream bug.
## ( http://free.nchc.org.tw/clonezilla-live/stable/Known-issues-Clonezilla-live.txt )
## If you are using French Keymap, Remember to use "Select keymap from full list"
## 1. Select keymap from full list
## 2. pc / azerty / French / Same as X11 (latin 9) /Standard

## Alternative 1:
sudo dpkg-reconfigure keyboard-configuration
## Make reconfigured keyboard-configuration changes affect.
setupcon

## Alternative 2:
edit /etc/default/keyboard

## Alternative 3 (temporary until reboot):
setxkbmap de
</pre>

== Desktop Environment ==

'''Table:''' ''Desktop Keyboard Layout Change''

{| class="wikitable"
|-
! scope="col"| '''Platform'''
! scope="col"| '''Steps'''
|-
! scope="row"| XFCE
| 
* <code>Applications</code> -> <code>Settings</code> -> <code>Keyboard</code> -> <code>Layout</code> -> <code>disable "use system defaults"</code> -> <code>add your layout at the bottom</code> -> <code>remove the old layout</code> -> <code>close</code>
|-
! scope="row"| KDE
| 
* Keyboard search: <code>Start menu button</code> -> <code>search</code> -> <code>keyboard</code> -> <code>layout</code> -> <code>add yours and remove the default</code> <br />
* System settings: <code>KDE</code> -> <code>Start menu button</code> -> <code>Applications</code> -> <code>Settings</code> ->  <code>System Settings</code> -> <code>input devices</code> -> <code>layout</code> -> <code>add yours and remove the default</code> -> <code>apply</code>
|}

= {{q_project_name_short}} =

To correctly apply changes system-wide via <code>dom0</code>: <ref>The Qubes bug reporting [https://github.com/QubesOS/qubes-issues/issues/1396 the change does not propagate to other VMs] has now been resolved.</ref>

* <code>System Tools</code> -> <code>Keyboard</code> -> <code>Layout</code>

Alternatively, [https://www.qubes-os.org/doc/language-localization/ language localization input] can be configured on a per VM basis. 

If problems are encountered, see: [https://www.qubes-os.org/faq/#my-keyboard-layout-settings-are-not-behaving-correctly-what-should-i-do My keyboard layout settings are not behaving correctly. What should I do?] in the Qubes FAQ.

= Footnotes =
{{reflist|close=1}}

{{Footer}}

[[Category:Documentation]]
