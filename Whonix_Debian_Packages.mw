{{Header}}

{{Stable}}

= Whonix Debian Packages =
== Which ones are safe to remove? ==
Feel free to remove the following desktop specific meta packages.

<pre>
sudo apt-get remove whonix-shared-desktop
sudo apt-get remove whonix-shared-desktop-kde
sudo apt-get remove whonix-shared-kde-accessibility
</pre>

Feel free to remove the following meta packages, which contain a list of recommended (as in useful to have, not necessary to have them for some other reason):

<pre>
sudo apt-get remove whonix-shared-packages-recommended
sudo apt-get remove whonix-gateway-packages-recommended
sudo apt-get remove whonix-workstation-packages-recommended
sudo apt-get remove whonix-workstation-default-applications
</pre>

After removing these packages, it is recommended to run the following command, so the packages which the meta packages above installed get uninstalled.

<pre>
sudo apt-get autoremove
</pre>

{{Footer}}

[[Category:Documentation]]
